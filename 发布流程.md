# CathayPDG 发布流程

1. 改版本号：`pdg_core.py` 与 `gui.py` 里的 `APP_VERSION`
2. 自检：`py -3 pdg_core.py --selftest`、`py -3 pdg_batch.py --selftest`、`py -3 gui.py --selftest`
3. 打包：双击 `发版.bat`（= `py -3 fab.py`）→ `dist\Release\CathayPDG.exe` + `校验值.txt` + `发行说明_vX.Y.Z.md`
4. 便携版：`开发\runtime\`（便携 Python）随包分发；`启动.bat` 会优先用它
5. 上传：中国移动云盘 / 百度网盘（密码 2026）→ 再 `gh release create`
6. 回填 README 下载表与更新日志

坑：
- PyInstaller 的 `--name` 绝不能与源码包同名（本项目无同名包，仍建议 `--distpath` 指定项目外/子目录）
- 必须 `--collect-all tkinterdnd2`，并 `--hidden-import Crypto`（pyzipper 依赖）
- `config\passwords.txt` 要打进包（`--add-data config;config`）
- .bat 一律纯 ASCII + CRLF
